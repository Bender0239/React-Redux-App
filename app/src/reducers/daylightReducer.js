const initialState = {
    sunrise: "",
    sunset: "",
    dayLength: "",
    solarNoon: ""
}

export const daylightReducer = (state = initialState, action) => {
    switch (action.type) {
        default: 
            return state
    }
}